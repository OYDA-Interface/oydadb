name: oydadb
description: "database interface for oyda applications."
version: 1.0.2
homepage: https://github.com/OYDA-Interface

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  postgres: ^2.6.4
  dotenv: ^4.2.0
  flutter_dotenv: ^5.1.0
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
# flutter:
#   assets:
#     - .env
